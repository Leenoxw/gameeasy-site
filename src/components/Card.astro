---
interface Props {
  title: string;
  image: string;
  category: string;
}

const { title, image, category } = Astro.props;
---

<div class="game-card">
  <div class="image-container">
    <img src={image} alt={title} />
  </div>

  <div class="card-content">
    <div class="info-row">
      <h3>{title}</h3>
      <span class="game-type">{category}</span>
    </div>
    
    <a href="#" class="btn-download">
      <span>Download</span>
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
        <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v4"></path>
        <polyline points="7 10 12 15 17 10"></polyline>
        <line x1="12" y1="15" x2="12" y2="3"></line>
      </svg>
    </a>
  </div>
</div>

<style>
    .layout-ads {
        display: grid;
        grid-template-columns: 200px 1fr 200px;
        width: 100%;
        min-height: 100vh;
        background: #060612;
    }

  .game-card {
    background: #0f0f1a;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 12px;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
    backdrop-filter: blur(10px);
  }

  .image-container {
    width: 100%;
    aspect-ratio: 16 / 9; 
    overflow: hidden;
    background: #000;
  }

  .image-container img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s ease;
  }

  .card-content {
    padding: 16px;
    /* AJOUT ICI : Assure que le bouton reste dans les limites malgré le padding */
    box-sizing: border-box;
    width: 100%;
    display: flex;
    flex-direction: column;
  }

  .info-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 16px;
    width: 100%;
  }

  h3 {
    margin: 0;
    font-size: 1.1rem;
    color: #fff;
    font-weight: 700;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .game-type {
    font-size: 0.65rem;
    color: #8a4fff;
    background: rgba(138, 79, 255, 0.15);
    padding: 4px 10px;
    border-radius: 6px;
    border: 1px solid rgba(138, 79, 255, 0.3);
    text-transform: uppercase;
    font-weight: 800;
    letter-spacing: 0.5px;
  }

  .btn-download {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    /* MODIFICATION ICI : Utilisation de 100% avec box-sizing pour un alignement parfait */
    width: 100%;
    box-sizing: border-box;
    padding: 12px;
    background: #fff;
    color: #000;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 800;
    font-size: 0.85rem;
    text-transform: uppercase;
    transition: all 0.3s ease;
  }

  .game-card:hover {
    transform: translateY(-12px) scale(1.05);
    border-color: #8a4fff;
    box-shadow: 0 15px 45px rgba(138, 79, 255, 0.4);
    z-index: 10;
  }

  .game-card:hover .image-container img {
    transform: scale(1.12);
  }

  .btn-download:hover {
    background: #8a4fff;
    color: #fff;
    box-shadow: 0 0 20px rgba(138, 79, 255, 0.4);
  }
</style>