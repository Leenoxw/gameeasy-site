<header class="header-container">
    <nav class="navbar">
        <div class="nav-left">
            <a href="/" class="logo">GAM<span>EASY</span></a>
        </div>
        
        <ul class="nav-links" id="navLinks">
            <li><a href="/">Accueil</a></li>
            <li><a href="/cracks" class="highlight"><i class="fas fa-rocket"></i> Cracks</a></li>
            <li><a href="/tuto" class="btn-tuto"><i class="fas fa-book"></i> Tuto</a></li>
            <li><a href="https://discord.gg/YaP3akhcgQ"><i class="fab fa-discord"></i> Discord</a></li>
            <li><a href="/about">About</a></li>
            <li><a href="/donation" class="btn-don"><i class="fas fa-heart"></i> Don</a></li>
        </ul>

        <div class="nav-right">
            <div id="logged-out-view" style="display: none;">
                <a href="/inscription" class="btn-auth-nav">
                    <i class="fas fa-user-plus"></i> S'inscription/connection
                </a>
            </div>

            <div id="logged-in-view" style="display: none; align-items: center; gap: 15px;">
                <span id="user-email" style="color: #a0a0a0; font-size: 13px;"></span>
                <button id="logout-btn" class="btn-logout">
                    <i class="fas fa-sign-out-alt"></i>
                </button>
            </div>
            <a href="/subscribe" class="btn-premium">
                <i class="fas fa-crown"></i> Premium
            </a>
            <button class="burger-menu" id="burgerBtn">
                <span class="bar"></span>
                <span class="bar"></span>
                <span class="bar"></span>
            </button>
        </div>
    </nav>
</header>

<script>
    import { auth } from '../lib/firebase';
    import { onAuthStateChanged, signOut } from "firebase/auth";

    const loggedOutView = document.getElementById('logged-out-view');
    const loggedInView = document.getElementById('logged-in-view');
    const userEmailSpan = document.getElementById('user-email');
    const logoutBtn = document.getElementById('logout-btn');

    onAuthStateChanged(auth, (user) => {
        if (user) {
            // L'utilisateur est connecté
            if (loggedOutView) loggedOutView.style.display = 'none';
            if (loggedInView) loggedInView.style.display = 'flex';
            if (userEmailSpan) userEmailSpan.innerText = user.email;
        } else {
            // L'utilisateur est déconnecté
            if (loggedOutView) loggedOutView.style.display = 'block';
            if (loggedInView) loggedInView.style.display = 'none';
        }
    });

    // Gestion de la déconnexion
    logoutBtn?.addEventListener('click', async () => {
        await signOut(auth);
        window.location.href = "/";
    });
    const burgerBtn = document.getElementById('burgerBtn');
    const navLinks = document.getElementById('navLinks');

    if (burgerBtn && navLinks) {
        burgerBtn.addEventListener('click', () => {
            burgerBtn.classList.toggle('active');
            navLinks.classList.toggle('open');
            document.body.style.overflow = navLinks.classList.contains('open') ? 'hidden' : 'auto';
        });
    }
</script>

<style>
    .header-container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        z-index: 9999;
        background: rgba(6, 6, 18, 0.9);
        backdrop-filter: blur(12px);
        border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        padding: 12px 0;
    }
    
    .navbar { 
        display: flex; 
        align-items: center; 
        justify-content: space-between; 
        padding: 0 5%; 
        max-width: 1400px; 
        margin: 0 auto; 
    }

    .nav-left, .nav-right {
        flex: 1;
        display: flex;
        align-items: center;
    }
    .nav-right { justify-content: flex-end; }

    .logo { 
       font-size: 24px; 
       font-weight: 800; 
       color: white; 
       text-decoration: none;
       white-space: nowrap;
    }
    .logo span { color: #8a4fff; }

    .nav-links { 
        display: flex; 
        list-style: none; 
        gap: 10px; 
        margin: 0; 
        padding: 0;
        justify-content: center;
        flex: 2;
    }

    /* --- ANIMATION DES BOUTONS --- */
    .nav-links a { 
        text-decoration: none; 
        color: #efefef; 
        font-size: 14px; 
        font-weight: 600; 
        padding: 8px 14px; 
        border-radius: 8px; 
        background: rgba(255, 255, 255, 0.03); 
        border: 1px solid rgba(255, 255, 255, 0.08); 
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); /* Animation plus fluide */
        display: flex; 
        align-items: center; 
        gap: 8px;
        white-space: nowrap;
        position: relative;
    }

    /* Effet de survol général */
    .nav-links a:hover {
        transform: translateY(-3px) scale(1.05);
        background: rgba(255, 255, 255, 0.1);
        border-color: rgba(255, 255, 255, 0.2);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
    }

    /* Hover spécifique pour le bouton Cracks (Violet) */
    .nav-links a.highlight { background: #8a4fff !important; color: white !important; border: none; }
    .nav-links a.highlight:hover {
        box-shadow: 0 0 20px rgba(138, 79, 255, 0.6);
    }

    .nav-links a.btn-tuto { border: 1px solid #8a4fff !important; color: #8a4fff !important; }

    /* STYLE DU BOUTON DON (ORANGE) */
    .nav-links a.btn-don {
        background: linear-gradient(135deg, #ff8c00, #f97316) !important;
        border: none !important;
        color: white !important;
        box-shadow: 0 4px 15px rgba(249, 115, 22, 0.3);
    }

    .nav-links a.btn-don:hover {
        box-shadow: 0 0 20px rgba(249, 115, 22, 0.6);
    }

    /* --- BURGER --- */
    .burger-menu {
        display: none;
        cursor: pointer;
        background: none;
        border: none;
        padding: 5px;
        flex-direction: column;
        gap: 5px;
    }
    .bar { width: 25px; height: 2px; background: white; transition: 0.3s; }

    /* --- MOBILE --- */
    @media (max-width: 1024px) {
        .nav-links {
            display: none;
            position: fixed;
            top: 0;
            right: 0;
            width: 100%;
            height: 100vh;
            background: #060612;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 20px;
            z-index: 10000;
        }

        .nav-links.open { display: flex; }

        .nav-links a {
            font-size: 20px;
            padding: 15px 40px;
            width: 250px;
            justify-content: center;
        }

        .burger-menu { display: flex; z-index: 10001; }

        .burger-menu.active .bar:nth-child(1) { transform: translateY(7px) rotate(45deg); }
        .burger-menu.active .bar:nth-child(2) { opacity: 0; }
        .burger-menu.active .bar:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }
        
        .nav-left { flex: 2; }
    }
    /* STYLE DU BOUTON PREMIUM (OR) */
    .btn-premium {
        text-decoration: none; 
        color: #060612 !important; /* Texte sombre pour contraster avec l'or */
        font-size: 13px; 
        font-weight: 800; 
        padding: 8px 16px; 
        border-radius: 8px; 
        background: linear-gradient(135deg, #ffca28, #ff8f00); /* Dégradé doré */
        display: flex; 
        align-items: center; 
        gap: 6px;
        margin-right: 15px; /* Espace avant le menu burger */
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        text-transform: uppercase;
        box-shadow: 0 4px 15px rgba(255, 143, 0, 0.3);
    }

    .btn-premium:hover {
        transform: translateY(-2px);
        box-shadow: 0 0 20px rgba(255, 202, 40, 0.5);
        filter: brightness(1.1);
    }

    /* Ajustement pour mobile */
    @media (max-width: 1024px) {
        .btn-premium {
            padding: 6px 12px;
            font-size: 12px;
            margin-right: 10px;
        }
    }
    /* STYLE DU BOUTON INSCRIPTION DANS LE HEADER */
    .btn-auth-nav {
        text-decoration: none;
        color: white;
        font-size: 13px;
        font-weight: 600;
        padding: 8px 16px;
        border-radius: 8px;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        display: flex;
        align-items: center;
        gap: 8px;
        margin-right: 10px;
        transition: all 0.3s ease;
        white-space: nowrap;
    }

    .btn-auth-nav:hover {
        background: rgba(255, 255, 255, 0.1);
        border-color: #8a4fff; /* Lueur violette au survol */
        transform: translateY(-2px);
    }

    .btn-auth-nav i {
        color: #8a4fff; /* Icône violette pour rappeler le logo */
    }

    /* Ajustement mobile pour cacher le bouton texte sur très petit écran si besoin 
       ou réduire sa taille */
    @media (max-width: 1024px) {
        .btn-auth-nav {
            padding: 6px 10px;
            font-size: 11px;
            margin-right: 8px;
        }
    }
    
    /* Si l'écran est vraiment petit (iPhone), on peut cacher le texte pour ne garder que l'icône */
    @media (max-width: 480px) {
        .btn-auth-nav span, .btn-auth-nav {
            display: none; /* Cache le bouton inscription sur mobile très petit pour éviter de surcharger */
        }
    }
    .btn-logout {
        background: rgba(255, 59, 59, 0.1);
        border: 1px solid rgba(255, 59, 59, 0.2);
        color: #ff3b3b;
        padding: 8px;
        border-radius: 8px;
        cursor: pointer;
        transition: 0.3s;
    }
    .btn-logout:hover {
        background: #ff3b3b;
        color: white;
    }
</style>