---
import Welcome from '../components/Welcome.astro'; 
import Layout from '../layouts/Layout.astro';
---
<section class="hero">
    <div class="hero-content">
        <h1>VOS JEUX <br> PRÉFÉRÉS <br> <span class="gradient-text">GRATUITEMENT</span></h1>
        <p>Découvrez un monde sans limite en illimité !</p>
        <div class="cta-buttons">
            <a href="cracks" class="btn-main">Découvrir le catalogue</a>
            <a href="#" class="btn-outline">Télécharger</a>
        </div>
    </div>
</section>

<style>
    .hero {
        height: 100vh;
        background: linear-gradient(to bottom, rgba(6, 6, 18, 0.5), #060612), 
                    url('https://images.unsplash.com/photo-1614850523296-d8c1af93d400?q=80&w=2070') no-repeat center center/cover;
        display: flex; align-items: center; padding-left: 8%;
    }
    h1 { font-size: 5rem; line-height: 0.9; margin: 0; font-weight: 900; }
    .cta-buttons { display: flex; gap: 20px; margin-top: 30px; }
    .btn-main { background: #8a4fff; padding: 15px 30px; border-radius: 10px; text-decoration: none; color: white; font-weight: bold; }
    .btn-outline { border: 1px solid #444; padding: 15px 30px; border-radius: 10px; text-decoration: none; color: white; backdrop-filter: blur(10px); }
</style>
<style>
    .cta-buttons a {
        text-decoration: none; /* Sécurité anti-soulignement */
        display: inline-flex;
        align-items: center;
        gap: 10px;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .btn-main {
        background: linear-gradient(135deg, #6366f1, #8a4fff);
        padding: 16px 32px;
        border-radius: 12px; /* Rectangle bien arrondi */
        color: white;
        font-weight: bold;
        box-shadow: 0 4px 15px rgba(138, 79, 255, 0.3);
    }

    .btn-outline {
        border: 1px solid rgba(255, 255, 255, 0.2);
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(10px);
        padding: 16px 32px;
        border-radius: 12px;
        color: white;
    }

    .cta-buttons a:hover {
        transform: scale(1.06); /* Petit effet de zoom au survol */
    }