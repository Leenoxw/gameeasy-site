---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="À Propos - GAMEASY">
  <Header />

  <main class="about-page">
    <div class="particles-container">
      {[...Array(15)].map((_, i) => <div class="particle" style={`--top: ${Math.random() * 100}%; --left: ${Math.random() * 100}%; --duration: ${10 + Math.random() * 20}s; --delay: ${Math.random() * 5}s;`}></div>)}
    </div>

    <div class="container">
      <section class="hero-section">
        <div class="badge">L'élite du Gaming</div>
        <h1 class="main-title">GAME<span>ASY</span></h1>
        <p class="tagline">Accédez à l'illimité. Testez. Jouez. Soutenez.</p>
        <div class="scroll-down">
          <div class="mouse"></div>
        </div>
      </section>

      <div class="about-grid">
        
        <div class="glass-card mission">
          <div class="icon-header">
            <i class="fas fa-microchip"></i>
            <h2>Notre Vision</h2>
          </div>
          <p>
            Depuis sa création en 2024, <strong>GAMEASY</strong> s'impose comme un pilier de la culture vidéoludique accessible. Nous ne sommes pas de simples hébergeurs, nous sommes des curateurs de technologie. 
          </p>
          <p>
            Notre objectif est de permettre à chaque joueur de tester les performances techniques des derniers titres sur sa configuration avant de passer à l'achat officiel.
          </p>
        </div>

        <div class="stats-grid">
          <div class="stat-box">
            <div class="stat-circle">
              <span class="num" data-val="500">500</span><span class="plus">+</span>
            </div>
            <p>Jeux Indexés</p>
          </div>
          <div class="stat-box">
            <div class="stat-circle">
              <span class="num" data-val="50">50</span>
            </div>
            <p>Membres Discord</p>
          </div>
        </div>

        <div class="glass-card values">
          <div class="icon-header">
            <i class="fas fa-shield-halved"></i>
            <h2>Sécurité & Engagement</h2>
          </div>
          <div class="val-item">
            <div class="v-icon"><i class="fas fa-vial"></i></div>
            <div class="v-txt">
              <strong>Machine Virtuelle</strong>
              <p>Chaque fichier est testé manuellement dans un bac à sable isolé.</p>
            </div>
          </div>
          <div class="val-item">
            <div class="v-icon"><i class="fas fa-tachometer-alt"></i></div>
            <div class="v-txt">
              <strong>Vitesse de pointe</strong>
              <p>Nous indexons uniquement des liens à haut débit (No-Limit).</p>
            </div>
          </div>
        </div>

        <div class="final-cta">
          <div class="cta-inner">
            <h2>Rejoignez la communauté</h2>
            <p>Ne restez pas seul. Obtenez de l'aide, partagez vos pseudos et jouez avec nous.</p>
            <div class="btn-group">
              <a href="https://discord.gg/YaP3akhcgQ" target="_blank" class="btn-discord">
                <i class="fab fa-discord"></i> Serveur Discord
              </a>
              <a href="/" class="btn-ghost">Catalogue complet</a>
            </div>
          </div>
        </div>

      </div>
    </div>
  </main>

  <Footer />
</Layout>

<style>
  /* CONFIGURATION GÉNÉRALE */
  .about-page {
    background: #04040c;
    color: white;
    min-height: 100vh;
    padding: 120px 20px 100px;
    font-family: 'Inter', sans-serif;
    position: relative;
    overflow: hidden;
  }

  .container { max-width: 1100px; margin: 0 auto; position: relative; z-index: 5; }

  /* PARTICULES */
  .particles-container { position: absolute; inset: 0; z-index: 1; pointer-events: none; }
  .particle {
    position: absolute;
    top: var(--top); left: var(--left);
    width: 3px; height: 3px;
    background: #8a4fff;
    border-radius: 50%;
    filter: blur(1px);
    opacity: 0.2;
    animation: float var(--duration) linear infinite var(--delay);
  }
  @keyframes float {
    0% { transform: translateY(0) scale(1); opacity: 0; }
    50% { opacity: 0.5; }
    100% { transform: translateY(-100vh) scale(1.5); opacity: 0; }
  }

  /* HERO SECTION */
  .hero-section { text-align: center; margin-bottom: 80px; }
  .badge {
    display: inline-block;
    background: rgba(138, 79, 255, 0.1);
    color: #8a4fff;
    padding: 8px 20px;
    border-radius: 50px;
    font-size: 0.8rem;
    font-weight: 800;
    text-transform: uppercase;
    border: 1px solid rgba(138, 79, 255, 0.2);
    margin-bottom: 25px;
  }
  .main-title { font-size: clamp(3rem, 10vw, 6rem); font-weight: 950; letter-spacing: -2px; line-height: 1; }
  .main-title span { color: #8a4fff; text-shadow: 0 0 40px rgba(138, 79, 255, 0.5); }
  .tagline { color: #888; font-size: 1.2rem; margin-top: 15px; }

  /* MOUSE SCROLL */
  .mouse { width: 25px; height: 40px; border: 2px solid #333; border-radius: 20px; margin: 40px auto; position: relative; }
  .mouse::before { content: ''; width: 4px; height: 8px; background: #8a4fff; position: absolute; left: 50%; top: 8px; transform: translateX(-50%); border-radius: 2px; animation: scroll 2s infinite; }
  @keyframes scroll { 0% { opacity: 1; top: 8px; } 100% { opacity: 0; top: 25px; } }

  /* GRID SYSTEM */
  .about-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 30px; }

  /* CARDS */
  .glass-card {
    background: rgba(255, 255, 255, 0.02);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.05);
    border-radius: 32px;
    padding: 40px;
    transition: 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }
  .glass-card:hover { transform: translateY(-10px); border-color: rgba(138, 79, 255, 0.4); background: rgba(138, 79, 255, 0.02); }

  .icon-header { display: flex; align-items: center; gap: 20px; margin-bottom: 25px; }
  .icon-header i { font-size: 2rem; color: #8a4fff; }
  .icon-header h2 { font-size: 1.8rem; font-weight: 800; margin: 0; }

  .glass-card p { color: #aaa; line-height: 1.8; margin-bottom: 15px; }

  /* STATS */
  .stats-grid { display: flex; flex-direction: column; gap: 30px; }
  .stat-box {
    background: linear-gradient(135deg, rgba(138, 79, 255, 0.1), transparent);
    border-radius: 32px; padding: 30px; text-align: center;
    border: 1px solid rgba(138, 79, 255, 0.1);
  }
  .stat-circle { font-size: 4rem; font-weight: 900; color: #fff; line-height: 1; }
  .stat-circle .plus { color: #8a4fff; }
  .stat-box p { text-transform: uppercase; letter-spacing: 2px; font-weight: 700; color: #666; font-size: 0.8rem; margin-top: 10px; }

  /* VALEURS */
  .val-item { display: flex; gap: 20px; margin-bottom: 30px; }
  .v-icon { width: 50px; height: 50px; background: rgba(138, 79, 255, 0.1); border-radius: 14px; display: flex; align-items: center; justify-content: center; color: #8a4fff; font-size: 1.2rem; flex-shrink: 0; }
  .v-txt strong { display: block; margin-bottom: 5px; font-size: 1.1rem; }
  .v-txt p { font-size: 0.9rem; margin: 0; }

  /* CTA */
  .final-cta { grid-column: span 2; margin-top: 40px; }
  .cta-inner {
    background: linear-gradient(100deg, #8a4fff 0%, #5865F2 100%);
    border-radius: 40px; padding: 60px; text-align: center;
    box-shadow: 0 20px 50px rgba(138, 79, 255, 0.2);
  }
  .cta-inner h2 { font-size: 3rem; font-weight: 900; margin-bottom: 15px; }
  .cta-inner p { color: rgba(255,255,255,0.8); margin-bottom: 40px; }
  
  .btn-group { display: flex; gap: 20px; justify-content: center; flex-wrap: wrap; }
  .btn-discord, .btn-ghost { padding: 18px 40px; border-radius: 15px; font-weight: 800; text-decoration: none; transition: 0.3s; }
  .btn-discord { background: #fff; color: #5865F2; display: flex; align-items: center; gap: 10px; }
  .btn-ghost { background: rgba(0,0,0,0.2); color: #fff; border: 1px solid rgba(255,255,255,0.2); }
  .btn-discord:hover { transform: scale(1.05); }

  @media (max-width: 850px) {
    .about-grid { grid-template-columns: 1fr; }
    .final-cta { grid-column: span 1; }
    .cta-inner { padding: 40px 20px; }
    .main-title { font-size: 4rem; }
  }
</style>

<script>
  // Animation simple des compteurs au scroll
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const target = entry.target as HTMLElement;
        const val = parseInt(target.getAttribute('data-val') || "0");
        let start = 0;
        const duration = 10000;
        const step = val / (duration / 16);
        
        const update = () => {
          start += step;
          if (start < val) {
            target.innerText = Math.floor(start).toString();
            requestAnimationFrame(update);
          } else {
            target.innerText = val.toString();
          }
        };
        update();
        observer.unobserve(target);
      }
    });
  }, { threshold: 0.5 });

  document.querySelectorAll('.num').forEach(n => observer.observe(n));
</script>