---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Schéma d'Installation - GAM EASY">
  <Header />

  <main class="tuto-page">
    <div class="container">
      <section class="tuto-header">
        <h1>PARCOURS D'<span>INSTALLATION</span></h1>
        <p>Le chemin le plus rapide pour lancer votre jeu.</p>
      </section>

      <div class="roadmap">
        <div class="roadmap-line"></div>

        <div class="roadmap-step">
          <div class="bubble">1</div>
          <div class="content">
            <h3>Téléchargement</h3>
            <p>Cliquez sur <strong>Download</strong>. Nous conseillons uBlock pour éviter les pubs.</p>
          </div>
        </div>

        <div class="roadmap-step">
          <div class="bubble active">2</div>
          <div class="content">
            <h3>Extraction</h3>
            <p>Utilisez <span class="highlight-soft">WinRAR</span> ou <span class="highlight-soft">7-Zip</span> pour décompresser l'archive (MDP: 123).</p>
          </div>
        </div>

        <div class="roadmap-step">
          <div class="bubble">3</div>
          <div class="content">
            <h3>Installation</h3>
            <p>Lancez le <code>setup.exe</code> en <strong>Administrateur</strong> pour éviter les bugs.</p>
          </div>
        </div>

        <div class="roadmap-step">
          <div class="bubble final">
             <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
          </div>
          <div class="content">
            <h3>Jouer</h3>
            <p>Utilisez le raccourci bureau et profitez de votre jeu !</p>
          </div>
        </div>
      </div>

      <section class="softwares">
        <h2>Logiciels requis</h2>
        <div class="soft-grid">
          <a href="https://www.win-rar.com/" class="soft-card winrar">WinRAR</a>
          <a href="https://www.7-zip.org/" class="soft-card sevenzip">7-Zip</a>
        </div>
      </section>
    </div>
  </main>

  <Footer />
</Layout>

<style>
  .tuto-page { padding: 140px 20px 80px; background: #060612; min-height: 100vh; color: white; }
  .container { max-width: 900px; margin: 0 auto; }

  .tuto-header { text-align: center; margin-bottom: 80px; }
  h1 { font-size: 2.8rem; font-weight: 800; }
  h1 span { color: #8b5cf6; }

  /* Roadmap Schema */
  .roadmap { 
    position: relative; 
    display: flex; 
    flex-direction: column; 
    gap: 40px; 
    padding-left: 50px;
  }

  /* La ligne verticale qui relie les bulles */
  .roadmap-line { 
    position: absolute; 
    left: 19px; 
    top: 0; 
    bottom: 0; 
    width: 2px; 
    background: linear-gradient(to bottom, #8b5cf6 0%, #1e1e2e 100%);
  }

  .roadmap-step { 
    position: relative; 
    display: flex; 
    align-items: center; 
    transition: 0.3s;
  }

  .bubble { 
    position: absolute; 
    left: -50px; 
    width: 40px; 
    height: 40px; 
    background: #11111e; 
    border: 2px solid #8b5cf6; 
    border-radius: 50%; 
    display: flex; 
    align-items: center; 
    justify-content: center; 
    font-weight: bold; 
    z-index: 2;
    box-shadow: 0 0 15px rgba(139, 92, 246, 0.3);
  }

  .bubble.active { background: #8b5cf6; color: white; }
  .bubble.final { background: #10b981; border-color: #10b981; box-shadow: 0 0 15px rgba(16, 185, 129, 0.4); }

  .content { 
    background: #11111e; 
    padding: 20px 30px; 
    border-radius: 15px; 
    border: 1px solid #1e1e2e; 
    width: 100%;
    transition: 0.3s;
  }
  .roadmap-step:hover .content { border-color: #8b5cf6; transform: translateX(10px); }

  h3 { margin-bottom: 5px; color: #8b5cf6; }
  p { color: #a1a1aa; font-size: 0.95rem; }

  /* Surlignage WinRAR / 7Zip */
  .highlight-soft { 
    background: rgba(139, 92, 246, 0.2); 
    color: #c4b5fd; 
    padding: 2px 6px; 
    border-radius: 4px; 
    font-weight: bold;
    border-bottom: 2px solid #8b5cf6;
  }

  /* Section logiciels du bas */
  .softwares { margin-top: 80px; text-align: center; }
  .softwares h2 { margin-bottom: 30px; font-size: 1.5rem; }
  .soft-grid { display: flex; gap: 20px; justify-content: center; }

  .soft-card { 
    padding: 15px 40px; 
    border-radius: 10px; 
    text-decoration: none; 
    color: white; 
    font-weight: bold; 
    transition: 0.3s;
    border: 2px solid transparent;
  }

  .winrar { background: #2a2a4a; border-color: #8b5cf6; box-shadow: 0 0 20px rgba(139, 92, 246, 0.2); }
  .sevenzip { background: #2a2a4a; border-color: #8b5cf6; box-shadow: 0 0 20px rgba(139, 92, 246, 0.2); }
  
  .soft-card:hover { transform: scale(1.05); filter: brightness(1.2); }

  @media (max-width: 600px) {
    .roadmap { padding-left: 40px; }
    .bubble { left: -40px; width: 30px; height: 30px; font-size: 0.8rem; }
    .roadmap-line { left: 14px; }
    h1 { font-size: 2rem; }
  }
</style>