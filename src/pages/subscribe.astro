---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Abonnement - GAM EASY">
    <Header />

    <main>
        <section class="sub-page">
            <div class="sub-header">
                <h1>Passez au niveau <span>supérieur</span></h1>
                <p>Choisissez le plan qui vous correspond et profitez d'une expérience sans limites.</p>
            </div>

            <div class="pricing-container">
                <div class="pricing-card">
                    <h3>Basique</h3>
                    <div class="price">0€<span>/mois</span></div>
                    <ul class="features">
                        <li>Accès aux jeux gratuits</li>
                    </ul>
                    <a href="/" class="btn-sub">Continuer</a>
                </div>

                <div class="pricing-card popular">
                    <div class="badge">Recommandé</div>
                    <h3>Premium</h3>
                    <div class="price">2.99€<span>/mois</span></div>
                    <ul class="features">
                        <li><strong>Zéro publicité</strong></li>
                        <li>Accès prioritaire aux jeux</li>
                    </ul>
                    <a href="#" class="btn-sub btn-gold">Devenir Premium</a>
                </div>

                <div class="pricing-card">
                    <h3>Ultimate</h3>
                    <div class="price">4.99€<span>/mois</span></div>
                    <ul class="features">
                        <li><strong>demande de nouveau jeu dans le catalogue avec un mail</strong></li>
                        <li>Zéro publicité</li>
                        <li>réponses aux mails immédiat</li>
                    </ul>
                    <a href="#" class="btn-sub">Choisir Ultimate</a>
                </div>
            </div>
        </section>
    </main>

    <Footer />
</Layout>

<style>
    html, body {
        margin: 0;
        padding: 0;
        background-color: #060612; /* On met le fond de page à la même couleur que la section */
        overflow-x: hidden; /* Évite les barres de défilement horizontales bizarres */
        width: 100%;
    }
    * {
        box-sizing: border-box; /* Pour que le padding ne casse pas la largeur */
    }
    @keyframes fadeInUp {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
    }
    @keyframes goldGlow {
        0% { box-shadow: 0 0 10px rgba(255, 202, 40, 0.2); }
        50% { box-shadow: 0 0 25px rgba(255, 202, 40, 0.5); }
        100% { box-shadow: 0 0 10px rgba(255, 202, 40, 0.2); }
    }
    .sub-page {
        padding: 120px 5% 60px;
        min-height: 100vh;
        width: 100%;
        background: #060612;
        color: white;
        font-family: 'Inter', sans-serif;
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    .sub-header {
        text-align: center;
        margin-bottom: 50px;
        opacity: 0;
        animation: fadeInUp 0.8s ease-out forwards;
        animation-delay: 0.1s;
    }

    .sub-header h1 { font-size: 2.5rem; font-weight: 800; }
    .sub-header h1 span { color: #8a4fff; }
    .sub-header p { color: #a0a0a0; margin-top: 10px; }

    .pricing-container {
        display: flex;
        gap: 30px;
        flex-wrap: wrap;
        justify-content: center;
        max-width: 1200px;
        width: 100%;
    }

    .pricing-card {
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 20px;
        padding: 40px;
        width: 320px;
        display: flex;
        flex-direction: column;
        position: relative;
        opacity: 0;
        animation: fadeInUp 0.8s cubic-bezier(0.4, 0, 0.2, 1) forwards;
        transition: transform 0.3s ease, background 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease;
    }

    .pricing-card:nth-child(1) { animation-delay: 0.3s; }
    .pricing-card:nth-child(2) { animation-delay: 0.5s; }
    .pricing-card:nth-child(3) { animation-delay: 0.7s; }

    .pricing-card:hover {
        transform: translateY(-10px);
        background: rgba(255, 255, 255, 0.05);
        border-color: rgba(255, 255, 255, 0.2);
    }

    .pricing-card.popular {
        border: 1px solid #ffca28;
        background: rgba(255, 202, 40, 0.02);
        animation: fadeInUp 0.8s cubic-bezier(0.4, 0, 0.2, 1) forwards, 
                   goldGlow 3s ease-in-out infinite 1.5s;
    }

    .badge {
        position: absolute;
        top: -15px;
        left: 50%;
        transform: translateX(-50%);
        background: linear-gradient(135deg, #ffca28, #ff8f00);
        color: #060612;
        padding: 5px 15px;
        border-radius: 20px;
        font-size: 12px;
        font-weight: 800;
        text-transform: uppercase;
    }

    .pricing-card h3 { font-size: 1.5rem; margin-bottom: 15px; }
    .price { font-size: 2.5rem; font-weight: 800; margin-bottom: 25px; }
    .price span { font-size: 1rem; color: #a0a0a0; }

    .features {
        list-style: none;
        padding: 0;
        margin-bottom: 30px;
        flex-grow: 1;
    }

    .features li {
        margin-bottom: 12px;
        color: #d0d0d0;
        font-size: 14px;
        display: flex;
        align-items: center;
        gap: 10px;
    }
    .fa-check { color: #4caf50; }
    .fa-times { color: #f44336; }
    .btn-sub {
        text-align: center;
        text-decoration: none;
        color: white;
        padding: 12px;
        border-radius: 10px;
        background: rgba(255, 255, 255, 0.1);
        font-weight: 600;
        transition: 0.3s;
    }

    .btn-sub:hover { background: #ffca28; }

    .btn-gold {
        background: linear-gradient(135deg, #ffca28, #ff8f00);
        color: #060612;
    }

    .btn-gold:hover {
        box-shadow: 0 0 20px rgba(255, 202, 40, 0.4);
        transform: scale(1.02);
    }

    @media (max-width: 768px) {
        .pricing-card { width: 100%; }
    }
    .sub-page {
        padding: 120px 5% 60px;
        min-height: 100vh;
        background: #060612;
        color: white;
        display: flex;
        flex-direction: column;
        align-items: center;
    }
</style>